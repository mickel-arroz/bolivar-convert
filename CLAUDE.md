Read PROJECT_CONTEXT.md in the root directory for all project rules, tech stack, and coding standards.

## Agent skills

### Issue tracker

Issues live as GitHub issues in `mickel-arroz/bolivar-convert`, managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles, each label string equal to its name. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.
